extends ../layout 

block content
  h1 New Book 

  include includes/book-form-errors

  form(action="/books/new", method="post")
    include includes/book-form
    p
      input(type="submit" value="Create New Book")
    p 
      a.button(href="/books") Cancel
